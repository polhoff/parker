WipeDump <-
function()

	{

	setwd(dirdmp)

	file.remove(list.files())

	setwd(dirdmp1)

	file.remove(list.files())

	setwd(dirdmp2)

	file.remove(list.files())

	#setwd(dirdmp3)

	#file.remove(list.files())

	setwd(dirdmp)
	
	}
